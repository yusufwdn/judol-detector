# requirements.txt
# Install all dependencies: pip install -r requirements.txt

# Core ML
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=1.24.0

# Model serialization (save/load)
joblib>=1.3.0

# API Server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# Text preprocessing
emoji>=2.0.0        # emoji demojize (convert emoji to text tokens)
PySastrawi>=1.2.0   # Indonesian stemmer (used in experiment_stemming.py)

# Development & Exploration
jupyter>=1.0.0
matplotlib>=3.7.0   # confusion matrix visualization
seaborn>=0.12.0     # heatmap for confusion matrix
