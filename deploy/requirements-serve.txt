# requirements-serve.txt
# Dependency MINIMAL untuk menjalankan src/server.py di VPS.
# Beda dengan requirements.txt di root (itu untuk training/eksperimen: jupyter,
# matplotlib, seaborn, PySastrawi, dll — semua itu TIDAK dibutuhkan saat serving
# dan hanya bikin VPS boros RAM/disk).
#
# Versi scikit-learn di bawah SUDAH dicek dari env/ lokal (yang dipakai untuk
# melatih svm_model.joblib) pada 2026-08-08: scikit-learn==1.7.2
# Kalau nanti model dilatih ulang dengan versi env berbeda, cek ulang dengan:
#   env/Scripts/python -c "import sklearn; print(sklearn.__version__)"
# dan update baris scikit-learn di bawah SEBELUM install di VPS.

fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
scikit-learn==1.7.2
numpy>=1.24.0
scipy
joblib>=1.3.0
emoji>=2.0.0
